# 头条批量读取验收（2026-09-10）

版本范围：在原单篇读取器上增加 `read-toutiao-batch` 与宿主浏览器的关键词/账号发现说明。

## 现场样本

- 关键词「人工智能」：浏览器从头条首页搜索，进入资讯页；首页10条中按日期初筛出3条近期候选，批量命令3/3读到正文并通过24小时复核。
- 文章编号：7683705619378946583、7683561825191707188、7683764606787732010；正文分别3742、2562、1260字符。
- 账号「中国网信杂志」：核对主页认证，等待实际列表，选文章标签和主列表前5篇；批量读取5/5成功，正文分别1084、997、529、1543、8697字符。
- 账号主页先显示加载失败及零计数，随后无刷新加载成功。这只证明存在加载过程中的暂态，未认定网站所有失败都有相同原因。
- 所有样本均为移动文章接口；未依赖 Jina 后备。核对列表标题、正文编号及结构，不代表与作者原稿逐字比较。

## 范围

搜索只覆盖这次首屏10条，没有遍历全部页；账号只采集首批5篇。批次处理完成与全量发现分开记录。
搜索发现依赖宿主浏览器能力，独立 CLI 不会自行登录、搜索或操作浏览器。
正文中的图片只保留引用，不声称识别了图片或视频内容。

## 自动检查

完整测试679项通过，Ruff和mypy通过。新增测试覆盖去重、续跑不重复请求、查询条件变更保护、
正文日期筛选、账号不默认过滤旧文、失败保留、访问拒绝停止、标题不一致与安全解析搜索跳转链接。
本地原始正文与登录资料不提交仓库。
